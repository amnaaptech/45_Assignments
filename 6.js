//6th question
//striping names : store a person's name and include some, whitespeace characters at the beginning and end the of the name, Make sure you use each character combination "\t", "\n" and atleast once , Print the name once so the whitespeace around the name is displayed ,the Print the name after striping the whitespaces  
let Name = `\n \t Amna Sheikh \t \n`;
console.log(Name);
let stripped = Name.trim();
console.log(stripped);
export {};
